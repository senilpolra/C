#include <stdio.h>

int main()
{
    char str[200];
    int i, count = 0;

    printf("enter name: ");
    scanf("%[^\n]s", &str);

    for (i = 0; str[i] != '\0'; i++)
    {
        if (str[i] == ' ')
        {
            
        }
    }
}